source /usr/local/share/jvm/jvm-select-common

# configure naming for YUMmy distros
function jvmSelect(){
  flavor=$1
  version=$2
  arch=${3-$(arch)}
  archSuffix=".$arch"
  versionPrefix="1."
  versionSuffix=".0"
  jvmSelectCommon
}
