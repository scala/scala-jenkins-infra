---
# TODO: lrytz, retronym, SethTisue, szeiger
- name: Give adriaanm's github pubkey admin access
  authorized_key:
    user: "{{ admin_user }}"
    state: present
    key: https://github.com/adriaanm.keys

- name: Install basic packages
  apt: name={{item}} state=installed
  with_items:
       - apt-transport-https
       - openjdk-8-jdk
       - git
